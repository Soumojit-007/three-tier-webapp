apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: teamhub-ingress
  namespace: teamhub

spec:
  ingressClassName: nginx

  rules:
    - host: teamhub.local

      http:
        paths:
          - path: /
            pathType: Prefix

            backend:
              service:
                name: frontend-service
                port:
                  number: 80